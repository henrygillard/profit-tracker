---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.config.js
  - tests/__mocks__/prisma.js
  - tests/webhooks.test.js
  - tests/auth.test.js
  - tests/env.test.js
  - tests/scopes.test.js
  - package.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Running `npx jest` produces test output (not 'command not found')"
    - "All test files exist and fail with 'not implemented' or assertion failures — not import errors"
    - "Prisma is mocked so integration tests do not require a database connection"
  artifacts:
    - path: "jest.config.js"
      provides: "Test runner configuration"
      contains: "testEnvironment"
    - path: "tests/__mocks__/prisma.js"
      provides: "Mocked Prisma client for integration tests"
      exports: ["prisma"]
    - path: "tests/webhooks.test.js"
      provides: "Failing test scaffolds for FOUND-01 GDPR handlers"
    - path: "tests/auth.test.js"
      provides: "Failing test scaffolds for FOUND-04 JWT middleware"
    - path: "tests/env.test.js"
      provides: "Failing test scaffolds for FOUND-03 env validation"
    - path: "tests/scopes.test.js"
      provides: "Failing test scaffolds for FOUND-02 scope pruning"
  key_links:
    - from: "tests/webhooks.test.js"
      to: "routes/webhooks.js"
      via: "supertest app import"
      pattern: "require.*routes/webhooks|require.*server"
    - from: "tests/__mocks__/prisma.js"
      to: "lib/prisma.js"
      via: "Jest manual mock"
      pattern: "__mocks__/prisma"
---

<objective>
Install the Jest + Supertest test framework and write failing test scaffolds for all four Phase 1 requirements.

Purpose: Every subsequent plan in this phase depends on automated tests to verify implementation. The Nyquist rule requires all test files to exist before implementation begins. Tests are written to fail — green state is the goal of the implementation plans.

Output: A working test framework (`npx jest` runs), four test files with failing assertions, a mocked Prisma client, and updated package.json devDependencies.
</objective>

<execution_context>
@/Users/henry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/henry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Jest + Supertest and create test infrastructure</name>
  <files>package.json, jest.config.js, tests/__mocks__/prisma.js</files>
  <behavior>
    - `npx jest` runs without error ("jest: command not found" is a failure)
    - jest.config.js sets testEnvironment to "node" and testMatch to "tests/**/*.test.js"
    - tests/__mocks__/prisma.js exports a jest.fn()-mocked prisma client with at least shopSession.deleteMany mocked
    - Mock is a manual Jest mock so it auto-intercepts `require('../lib/prisma')` in test files
  </behavior>
  <action>
1. Run `npm install --save-dev jest supertest` in the project root to add devDependencies.

2. Create `jest.config.js` at project root:
```javascript
module.exports = {
  testEnvironment: 'node',
  testMatch: ['**/tests/**/*.test.js'],
  clearMocks: true,
};
```

3. Add test script to package.json scripts: `"test": "jest"`.

4. Create `tests/__mocks__/prisma.js` — this is a Jest manual mock that intercepts `require('./lib/prisma')` across all test files. The mock must export the same shape as the real module (`{ prisma: { shopSession: { deleteMany: jest.fn() } } }`). Because the mock lives in `tests/__mocks__/`, you must tell Jest where to look — add `roots: ['<rootDir>']` and `moduleNameMapper: { '^../lib/prisma$': '<rootDir>/tests/__mocks__/prisma.js', '^./lib/prisma$': '<rootDir>/tests/__mocks__/prisma.js' }` to jest.config.js so both relative paths resolve to the mock.

The mock file content:
```javascript
const prisma = {
  shopSession: {
    deleteMany: jest.fn().mockResolvedValue({ count: 0 }),
    findFirst: jest.fn().mockResolvedValue(null),
  },
};
module.exports = { prisma };
```
  </action>
  <verify>
    <automated>cd /Users/henry/code/profit-tracker && npx jest --listTests</automated>
  </verify>
  <done>jest lists test files (even 0 is fine at this step); `npx jest --listTests` exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Write failing test scaffolds for all four requirements</name>
  <files>tests/webhooks.test.js, tests/auth.test.js, tests/env.test.js, tests/scopes.test.js</files>
  <action>
Create four test files with real test cases that will fail until the implementation plans complete. Each test describes the expected behavior exactly — the test body should attempt the behavior and assert the outcome. Tests must fail due to "not implemented yet" behavior (stubs, missing middleware, wrong values), NOT due to import errors or syntax errors.

**tests/webhooks.test.js** — GDPR handlers (FOUND-01).
Uses supertest to POST to the webhook routes. Must import the express app (create a minimal test app from routes/webhooks.js). Uses the existing `verifyWebhookHmac` pattern: generate a valid HMAC using `crypto.createHmac('sha256', process.env.SHOPIFY_API_SECRET || 'test-secret').update(body).digest('base64')` for valid-HMAC tests, pass a garbage string for invalid-HMAC tests.

Test cases:
- `customers/redact with valid HMAC returns 200` — POST body `{"shop_domain":"test.myshopify.com","customer":{"id":12345}}`, HMAC computed from body, expect 200
- `customers/redact with invalid HMAC returns 401` — same body, HMAC = "invalid", expect 401
- `shop/redact with valid HMAC calls prisma.shopSession.deleteMany and returns 200` — POST body `{"myshopify_domain":"test.myshopify.com"}`, valid HMAC, expect 200, assert `prisma.shopSession.deleteMany` was called with `{ where: { shop: 'test.myshopify.com' } }`
- `customers/data_request with valid HMAC returns 200` — POST body `{"shop_domain":"test.myshopify.com"}`, valid HMAC, expect 200

To mount the router in supertest, create a minimal express app inside the test file:
```javascript
const express = require('express');
const request = require('supertest');
const webhookRouter = require('../routes/webhooks');
// Set test secret before requiring anything that reads env
process.env.SHOPIFY_API_SECRET = 'test-secret';
const app = express();
app.use(express.raw({ type: 'application/json' }));
app.use('/webhooks', webhookRouter);
```

**tests/auth.test.js** — JWT middleware (FOUND-04).
Must import `lib/verifySessionToken.js` (which doesn't exist yet — this will cause the test to fail with MODULE_NOT_FOUND until Plan 03 creates it, which is the intended failing state). Create the import inside a try/catch or use `jest.mock` so the test file itself loads cleanly even if the module is missing — use a `beforeAll` that requires the module and stores it, then tests skip if it's not found.

Actually, use a cleaner approach: wrap the require in a try/catch at the top of the file and set a `moduleLoaded` flag. Each test checks `if (!moduleLoaded) { expect(false).toBe(true) }` so tests fail clearly. This avoids import errors crashing the test file.

Test cases (all should fail until Plan 03 ships):
- `GET /api/health without Authorization header returns 401`
- `GET /api/health with valid JWT returns 200 and sets shopDomain`
- `GET /api/health with expired JWT returns 401`
- `GET /api/health with wrong aud claim returns 401`

To generate a valid test JWT: `const jwt = require('jsonwebtoken'); const token = jwt.sign({ iss: 'https://test.myshopify.com/admin', dest: 'https://test.myshopify.com', aud: process.env.SHOPIFY_API_KEY || 'test-key' }, process.env.SHOPIFY_API_SECRET || 'test-secret', { algorithm: 'HS256', expiresIn: '1h' });`

Mount a minimal test express app with the middleware on `/api/*` routes and a `GET /api/health` that returns `200 { shop: req.shopDomain }`.

**tests/env.test.js** — Env validation (FOUND-03).
Uses `child_process.spawnSync` to run `node server.js` with specific env vars removed. The test must NOT import server.js directly (that would start a server in the test process).

Test cases:
- `exits non-zero and prints error when SHOPIFY_API_KEY is missing` — spawn node server.js with all env vars except SHOPIFY_API_KEY, assert exit code !== 0, assert stderr includes 'SHOPIFY_API_KEY'
- `exits non-zero and prints error when DATABASE_URL is missing` — same but remove DATABASE_URL
- `exits non-zero and prints error when SHOPIFY_SCOPES is missing` — same but remove SHOPIFY_SCOPES (this one WILL fail until Plan 03 adds SHOPIFY_SCOPES to the REQUIRED_ENV array)

Use `spawnSync('node', ['server.js'], { env: { ...process.env, SHOPIFY_API_KEY: undefined }, cwd: '/Users/henry/code/profit-tracker', timeout: 3000 })` — set the missing key to undefined to remove it from the env object (filter out undefined keys before passing). Note: `spawnSync` env object ignores keys set to undefined; instead do `const env = {...process.env}; delete env['SHOPIFY_API_KEY']`.

**tests/scopes.test.js** — Scope pruning (FOUND-02).
Reads the toml file directly and asserts the scopes field contains only the expected values (or is empty).

Test case:
- `shopify.app.profit-tracker.toml has no excess scopes` — read the toml file with `fs.readFileSync`, parse the scopes value using a regex (`/scopes\s*=\s*"([^"]*)"/`), split by comma, filter empty strings. Assert that the resulting array has 0 elements (empty string means no scopes for Phase 1). This test will fail until Plan 04 sets `scopes = ""` in the toml.

After creating all four files, run `npx jest` — tests should run (no import crashes) and fail with assertion errors or module-not-found messages (not syntax errors). That is the desired state.
  </action>
  <verify>
    <automated>cd /Users/henry/code/profit-tracker && npx jest --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    All four test files exist. `npx jest` runs and exits (even if tests fail — failures are expected and correct). No test file crashes with syntax errors. At least webhooks.test.js and env.test.js produce assertion failures (not module errors) because routes/webhooks.js and server.js exist.
  </done>
</task>

</tasks>

<verification>
Run `npx jest` from project root. Expected: 4 test suites found, tests run, some fail (assertion failures expected). Zero test files should crash with syntax errors.

Run `npx jest --listTests` — should list all four test files.
</verification>

<success_criteria>
- `package.json` has `jest` and `supertest` in devDependencies
- `jest.config.js` exists with testEnvironment: 'node'
- `tests/__mocks__/prisma.js` exports mocked prisma with shopSession.deleteMany
- `tests/webhooks.test.js` exists with 4 test cases for GDPR handlers
- `tests/auth.test.js` exists with 4 test cases for JWT middleware
- `tests/env.test.js` exists with 3 test cases for env validation
- `tests/scopes.test.js` exists with 1 test case for scope pruning
- `npx jest` runs without crashing the process (test failures are fine)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
